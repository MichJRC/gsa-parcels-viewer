{
  "name": "GSA Parcels Viewer Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.13",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y gdal-bin && pip install -r requirements.txt && python -m ipykernel install --user",
  "postStartCommand": "python -m ipykernel install --user",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  },
  "forwardPorts": [8888]
}